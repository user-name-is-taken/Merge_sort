/*just an interface for the Worker and BaseCase

change this to have next() and getValue()
   value should just be an accessor
*/
public interface MergeSort{
   Integer getValue();
   void nextME();
}